{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "env":  ["NEXT_PUBLIC_NAVER_MAPS_CLIENT_ID", "NEXT_PUBLIC_NAVER_MAPS_CLIENT_SECRET"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  },
  "globalDependencies": [
    ".env", 
    "tsconfig.json"
  ],
  "globalEnv": ["NEXT_PUBLIC_NAVER_MAPS_CLIENT_ID", "NEXT_PUBLIC_NAVER_MAPS_CLIENT_SECRET"]
}
